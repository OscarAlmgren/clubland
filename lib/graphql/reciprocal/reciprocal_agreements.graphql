query ReciprocalAgreements($pagination: PaginationInput, $status: AgreementStatus) {
  reciprocalAgreements(pagination: $pagination, status: $status) {
    nodes {
      id
      clubId
      partnerClubId
      status
      agreementType
      agreementTerms {
        maxVisitsPerMonth
        reciprocalFee
        blackoutDates
        specialConditions
      }
      effectiveDate
      expirationDate
      startDate
      endDate
      createdAt
      updatedAt
    }
    pageInfo {
      page
      pageSize
      total
      totalPages
      hasNextPage
      hasPrevPage
    }
  }
}

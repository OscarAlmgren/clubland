mutation CreateNotification($input: SendNotificationInput!) {
  sendNotification(input: $input) {
    id
    userId
    title
    body
    message
    status
    channel
    priority
    scheduledFor
    createdAt
  }
}

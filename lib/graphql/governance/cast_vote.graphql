mutation CastVote($input: CastVoteInput!) {
  castVote(input: $input) {
    id
    proposalId
    voterId
    voteValue
    weight
    comments
    isAnonymous
    castAt
    createdAt
    voter {
      id
      firstName
      lastName
    }
  }
}

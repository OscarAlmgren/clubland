mutation CreateProposal($input: CreateProposalInput!) {
  createProposal(input: $input) {
    id
    title
    description
    type
    status
    category
    votingMethod
    votingStartTime
    votingEndTime
    quorumRequired
    approvalThreshold
    createdAt
  }
}

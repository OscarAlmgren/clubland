query SearchClubs(
  $query: String!
  $location: LocationInput
  $radius: Float
  $amenities: [String!]
  $pagination: PaginationInput
) {
  searchClubs(
    query: $query
    location: $location
    radius: $radius
    amenities: $amenities
    pagination: $pagination
  ) {
    nodes {
      id
      name
      slug
      description
      location
      logo
      distance
      relevanceScore
    }
    pageInfo {
      hasNextPage
      totalCount
    }
  }
}

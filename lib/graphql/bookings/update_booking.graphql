mutation UpdateBooking($id: ID!, $input: UpdateBookingInput!) {
  updateFacilityBooking(id: $id, input: $input) {
    id
    startTime
    endTime
    purpose
    guestCount
    specialRequests
    updatedAt
  }
}

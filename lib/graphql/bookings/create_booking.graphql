mutation CreateBooking($input: CreateBookingInput!) {
  createBooking(input: $input) {
    id
    clubId
    facilityId
    userId
    startTime
    endTime
    status
    notes
    facility {
      id
      name
      type
    }
    createdAt
  }
}

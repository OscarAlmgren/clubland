mutation CreateBooking($input: CreateBookingInput!) {
  createBooking(input: $input) {
    id
    facilityId
    userId
    startTime
    endTime
    status
    notes
    participants
    facility {
      name
      type
    }
    createdAt
  }
}

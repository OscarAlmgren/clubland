mutation CreateBooking($input: CreateBookingInput!) {
  createFacilityBooking(input: $input) {
    id
    clubId
    facilityId
    memberId
    startTime
    endTime
    status
    purpose
    guestCount
    facility {
      id
      name
      type
    }
    createdAt
  }
}

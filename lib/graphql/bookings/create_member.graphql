mutation CreateMember($input: CreateMemberInput!) {
  createMember(input: $input) {
    id
    clubId
    userId
    memberNumber
    membershipType
    status
    firstName
    lastName
    email
    phone
    joinedDate
    createdAt
    updatedAt
  }
}

mutation CheckOutVisit($input: CheckOutVisitInput!) {
  checkOutVisit(input: $input) {
    id
    memberId
    clubId
    visitingClubId
    status
    checkInTime
    checkOutTime
    services
    cost
    verified
    blockchainTxId
    createdAt
  }
}

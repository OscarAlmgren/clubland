mutation UpdateMember($id: ID!, $input: UpdateMemberInput!) {
  updateMember(id: $id, input: $input) {
    id
    clubId
    userId
    memberNumber
    membershipType
    status
    firstName
    lastName
    email
    phone
    dateOfBirth
    updatedAt
  }
}

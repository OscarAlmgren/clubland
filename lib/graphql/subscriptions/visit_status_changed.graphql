subscription VisitStatusChanged($clubId: ID) {
  visitStatusChanged(clubId: $clubId) {
    id
    memberId
    clubId
    visitingClubId
    status
    checkInTime
    checkOutTime
    verified
    createdAt
  }
}

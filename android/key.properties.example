# Android Release Signing Configuration
#
# Copy this file to key.properties (which is gitignored) and fill in your keystore details
#
# To generate a keystore, run:
# keytool -genkey -v -keystore ~/clubland-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias clubland

storeFile=/path/to/your/keystore.jks
storePassword=your_store_password
keyAlias=clubland
keyPassword=your_key_password
